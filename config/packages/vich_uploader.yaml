vich_uploader:

    db_driver: orm

    mappings:

        quran_session_images:

            uri_prefix: /uploads/sessionsCovers

            upload_destination: '%kernel.project_dir%/public/uploads/sessionsCovers'

            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer