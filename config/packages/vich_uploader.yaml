vich_uploader:

    db_driver: orm

    mappings:

        khatm_cover:

            uri_prefix: /uploads/khatm-covers

            upload_destination: '%kernel.project_dir%/public/uploads/khatm-covers'

            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer