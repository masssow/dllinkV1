{# Feature Card
    utilisation:
       <div class="col-md-6 col-lg-3">
        {% include 'components/_feature_card.html.twig' with {
            icon: '📖',
            title: 'Créer un khatm',
            text: 'Lancez une session de lecture collective en quelques étapes simples.'
            } %}
        </div>
 #}
<div class="bg-white rounded-4 shadow-sm p-4 h-100">
    <div class="fs-2 mb-3">{{ icon|default('✨') }}</div>
    <h5 class="fw-bold">{{ title }}</h5>
    <p class="text-muted small mb-0">{{ text }}</p>
</div>