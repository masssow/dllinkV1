<section id="cta" class="py-5 bg-success text-white">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-9 col-xl-8 text-center">

                <h2 class="fw-bold mb-3">
                    La communauté fait vivre DahiraLink
                </h2>

                <p class="mb-4 opacity-75">
                    Chaque session créée, chaque partage et chaque participation
                    contribue à faire vivre une dynamique collective utile et bénéfique.
                </p>

                <div class="d-flex flex-column flex-sm-row justify-content-center gap-3">
                    <a href="{{ path('app_quran_session_new') }}" class="btn btn-light btn-lg rounded-pill px-4">
                        Créer une session
                    </a>

                    {# <a href="{{ path('app_contact') }}" class="btn btn-outline-light btn-lg rounded-pill px-4">
                        Nous écrire
                    </a> #}
                </div>

            </div>
        </div>
    </div>
</section>